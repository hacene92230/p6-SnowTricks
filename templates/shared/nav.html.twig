<div class="container-fluid">
  <div class="row">
    <div class="col-6">
      <header>
        <nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
          <a class="navbar-brand" href="#">
            <img src="./logo.jpg" alt="SnowTrick" width="30" height="30">
            SnowTrick
          </a>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ml-auto">
              <li class="nav-item active">
                <a class="nav-link text-dark" href={{ path("home") }}>Accueil</a>
              </li>
              <li class="nav-item">
                <a class="nav-link text-dark" href="#">À propos</a>
              </li>
              <li class="nav-item">
                <a class="nav-link text-dark" href={{ path("home_contact") }}>Contact</a>
              </li>
              {% if app.user is empty%}
                <!-- Liens de connexion et d'inscription -->
                <li class="nav-item">
                  <a class="nav-link text-dark" href="{{ path('app_login') }}"><i class="fas fa-sign-in-alt"></i> Connexion</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link text-dark" href="{{ path('app_register') }}"><i class="fas fa-user-plus"></i> Inscription</a>
                </li>
              {% else %}
                <li class="nav-item">
                  {% include "shared/MenuUser.html.twig" %}
                </li>
              {% endif %}
            </ul>
          </div>
        </nav>
      </header>
    </div>
